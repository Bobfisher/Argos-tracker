export { ArgosTracker } from './tracker';
export { TrackerConfig, TrackEvent, UserInfo, ApiPerformanceProperties } from './types';
export { EventType, ReportMethod } from './constants';

// 默认导出，方便直接使用
import { ArgosTracker } from './tracker';
export default ArgosTracker;